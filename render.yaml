services:
  - type: web
    name: leadshield
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:prod
    startCommand: node server/index.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
